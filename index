<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menú con Estilo</title>
    <!-- Incluimos el CSS dentro de la etiqueta <style> -->
    <style>
        /* Estilos generales */
        body {
            background: #2196F3;
            font-family: sans-serif;
            font-size: 12px;
            color: #656565;
            position: relative;
            height: 300px;
            margin: 0 auto;
        }

        /* Estilo para el menú */
        .menu {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .menu ul {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        .menu li {
            margin: 0 10px;
        }

        .menu span {
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .menu span:hover {
            color: white;
        }

        /* Estilo para el botón */
        button {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            padding: 10px 20px;
            background: transparent;
            border: 2px solid white;
            color: white;
            font-size: 14px;
            cursor: pointer;
        }

        /* Estilo para el fondo semi-transparente */
        body::before {
            content: '';
            background: #B3E5FC;
            position: absolute;
            top: 0;  
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
    </style>
</head>
<body>
    <!-- Estructura HTML -->
    <div class="menu">
        <ul>
            <li><span>Home</span></li>
            <li><span>Works</span></li>
            <li><span>About</span></li>
            <li><span>Contact</span></li>
        </ul>
        <button>Toggle Menu</button>
    </div>

    <!-- Script JavaScript -->
    <script>
        // Seleccionamos los elementos necesarios
        var menu = document.querySelector('.menu');
        var button = document.querySelector('button');

        // Agregamos un evento al botón
        button.addEventListener('click', function (e) {
            e.preventDefault();
            menu.classList.toggle('active'); // Toggle la clase 'active' en el menú
        });
    </script>
</body>
</html>
